export default class Converter {
    constructor(coin) {
        this.coin = coin;
    }

    // getValues(coins) {
    //     const coin = coins.filter(c => c.id === this.id).map(c => ({name: c.name, symbol: c.symbol, price: c.quote.USD.price}));
    //     if(coin) this.coin = coin;
    // }
}